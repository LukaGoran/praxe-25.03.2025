# Třída, která reprezentuje pozici na herní mřížce
class Position:
    def __init__(self, row, column):
        self.row = row      # Řádek (svislá pozice)
        self.column = column  # Sloupec (vodorovná pozice)
